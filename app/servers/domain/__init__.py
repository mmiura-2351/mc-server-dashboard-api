"""Domain layer for the servers module.

Contains pure domain entities (`entities.py`), value objects
(`value_objects.py`), Port (Protocol) definitions (`ports.py`), and
domain exceptions (`exceptions.py`). Must not import any framework,
database driver, or HTTP client.

See `docs/ARCHITECTURE.md` §4.1.
"""
