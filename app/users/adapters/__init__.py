"""Adapter implementations for the users domain Ports.

These modules implement the Protocols defined in `app/users/domain/ports.py`.
They may depend on SQLAlchemy. They must not be imported by `domain/` or
`application/`.
"""
